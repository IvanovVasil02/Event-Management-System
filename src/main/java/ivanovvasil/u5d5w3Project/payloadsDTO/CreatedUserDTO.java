package ivanovvasil.u5d5w3Project.payloadsDTO;

public record CreatedUserDTO(Long id,
                             String name,
                             String surname,
                             String email,
                             String token) {
}
